#!/usr/bin/python

import sys
try:
	from scalrtools.cli import main
except ImportError, e:
	print >> sys.stderr, "error: %s\n\nPlease make sure that scalr is properly installed" % (e)
	sys.exit(1)

main()
